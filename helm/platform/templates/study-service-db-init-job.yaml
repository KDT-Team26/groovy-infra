apiVersion: batch/v1
kind: Job
metadata:
  name: study-service-db-init
  labels:
    app: study-service-db-init
  annotations:
    argocd.argoproj.io/hook: PreSync
    argocd.argoproj.io/hook-delete-policy: BeforeHookCreation
spec:
  backoffLimit: 3
  template:
    metadata:
      labels:
        app: study-service-db-init
    spec:
      restartPolicy: Never
      initContainers:
        - name: wait-for-mysql
          image: "{{ .Values.mysql.image.repository }}:{{ .Values.mysql.image.tag }}"
          imagePullPolicy: {{ .Values.mysql.image.pullPolicy }}
          command:
            - sh
            - -c
            - |
              i=0
              until mysqladmin ping -h mysql -u provisioner -p"$MYSQL_PWD" --silent; do
                i=$((i+1))
                if [ "$i" -ge 60 ]; then
                  echo "provisioner 계정에 60회 시도 후에도 접속 실패" >&2
                  exit 1
                fi
                sleep 3
              done
          env:
            - name: MYSQL_PWD
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: MYSQL_PROVISIONER_PASSWORD
      containers:
        - name: db-init
          image: "{{ .Values.mysql.image.repository }}:{{ .Values.mysql.image.tag }}"
          imagePullPolicy: {{ .Values.mysql.image.pullPolicy }}
          command:
            - sh
            - -c
            - mysql -h mysql -u provisioner < /sql/init.sql
          env:
            - name: MYSQL_PWD
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: MYSQL_PROVISIONER_PASSWORD
          volumeMounts:
            - name: init-sql
              mountPath: /sql
              readOnly: true
      volumes:
        - name: init-sql
          secret:
            secretName: db-init-sql
            items:
              - key: study-service.sql
                path: init.sql
