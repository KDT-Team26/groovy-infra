apiVersion: apps/v1
kind: Deployment
metadata:
  name: calendar-service
  labels:
    app: calendar-service
spec:
  # 롤아웃 안전장치(#90) — 상세 설명은 identity-service-deployment.yaml 참고.
  revisionHistoryLimit: {{ .Values.calendarService.rollout.revisionHistoryLimit }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: {{ .Values.calendarService.rollout.maxUnavailable }}
      maxSurge: {{ .Values.calendarService.rollout.maxSurge }}
  {{- if not .Values.calendarService.hpa.enabled }}
  replicas: {{ .Values.calendarService.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      app: calendar-service
  template:
    metadata:
      labels:
        app: calendar-service
    spec:
      containers:
        - name: calendar-service
          image: "{{ .Values.calendarService.image.repository }}:{{ .Values.calendarService.image.tag }}"
          imagePullPolicy: {{ .Values.calendarService.image.pullPolicy }}
          envFrom:
            - configMapRef:
                name: calendar-service-config
            - secretRef:
                name: calendar-service-secret
          env:
            - name: KAFKA_SASL_JAAS_CONFIG
              valueFrom:
                secretKeyRef:
                  name: kafka-secret
                  key: app-jaas-config
            - name: JAVA_TOOL_OPTIONS
              value: {{ .Values.calendarService.javaOpts | quote }}
          ports:
            - containerPort: {{ .Values.calendarService.port }}
              protocol: TCP
          # health probe(#90) — 상세 설명은 identity-service-deployment.yaml 참고.
          {{- if .Values.calendarService.probes.enabled }}
          startupProbe:
            {{- toYaml .Values.calendarService.probes.startup | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.calendarService.probes.readiness | nindent 12 }}
          livenessProbe:
            {{- toYaml .Values.calendarService.probes.liveness | nindent 12 }}
          {{- end }}
          {{- if .Values.calendarService.resources }}
          resources:
            {{- toYaml .Values.calendarService.resources | nindent 12 }}
          {{- end }}
