apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: groovy-redis-data
  labels:
    app: redis
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.redis.persistence.size }}
